/*let numero = 5; // O número da tabuada
let contador = 1; // Começa em 1

while (contador <= 10) { // Enquanto o contador for menor ou igual a 10
    console.log(numero + " x " + contador + " = " + (numero * contador)); // Imprime a tabuada
    contador++; // Aumenta o contador
}
*/
/*let numero = 5; // O número da tabuada

for (let contador = 1; contador <= 10; contador++) { // Loop de 1 a 10
    console.log(numero + " x " + contador + " = " + (numero * contador)); // Imprime o resultado da multiplicação
}
*/
/*
let numero = 5; // O número da tabuada
let contador = 1; // Começa em 1

do {
    console.log(numero + " x " + contador + " = " + (numero * contador)); // Imprime a tabuada
    contador++; // Aumenta o contador
} while (contador <= 10); // Continua até que contador seja maior que 10

*/ 