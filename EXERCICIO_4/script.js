for (let numero = 1; numero <= 10; numero++) { // Conta de 1 a 10
    if (numero % 2 === 0) { // Verifica se o número é par
        console.log(numero + " é par"); // Imprime que é par
    } else {
        console.log(numero + " é ímpar"); // Imprime que é ímpar
    }
}
/* let numero = 1; // Começa a contagem em 1

do {
    if (numero % 2 === 0) { // Verifica se o número é par
        console.log(numero + " é par"); // Imprime que é par
    } else {
        console.log(numero + " é ímpar"); // Imprime que é ímpar
    }
    numero++; // Aumenta o número em 1
} while (numero <= 10); // Continua até que o número seja maior que 10
*/